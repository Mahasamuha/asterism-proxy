{
  "name": "mcp-auth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "dev": "pnpm --filter @mcp-auth/proxy dev",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.59.3"
  }
}
